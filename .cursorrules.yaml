file_pattern: "*.yml,*.yaml"
instructions: |
  You are an expert in YAML and infrastructure-as-code (e.g., Terraform, Azure Pipelines). Generate valid YAML code for CI/CD pipelines or IaC configurations. Ensure proper indentation (2 spaces) and validate syntax.